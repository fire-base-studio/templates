# A Simple MCP OAuth Provider written in TypeScript (Deno Version)

This is a Deno port of the original project.

## Running the Project

To run the server, navigate to the `deno/oauth-provider` directory and execute the following command in your terminal:

```bash
deno task start
```
